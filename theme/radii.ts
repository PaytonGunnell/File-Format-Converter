// Border radius scale. Use these tokens everywhere instead of hardcoded values.

export interface RadiiTokens {
  sm: number;
  md: number;
  lg: number;
  pill: number;
}

export const radii: RadiiTokens = {
  sm: 8,
  md: 12,
  lg: 20,
  pill: 999,
};

export default radii;
